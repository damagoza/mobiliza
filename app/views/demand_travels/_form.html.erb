<%= form_for(@demand_travel) do |f| %>
  <% if @demand_travel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@demand_travel.errors.count, "error") %> prohibited this demand_travel from being saved:</h2>

      <ul>
      <% @demand_travel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :offer_travel_id %><br>
    <%= f.text_field :offer_travel_id %>
  </div>
  <div class="field">
    <%= f.label :accept %><br>
    <%= f.check_box :accept %>
  </div>
  <div class="field">
    <%= f.label :state %><br>
    <%= f.check_box :state %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
